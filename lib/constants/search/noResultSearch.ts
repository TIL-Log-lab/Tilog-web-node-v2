import LANGUAGE from "@Library/language/constant";

import { LanguageInterface } from "@Library/language/interface";

const NO_RESULT_SEARCH: LanguageInterface = {
  [LANGUAGE.ko]: "검색 결과가 없습니다.",
  [LANGUAGE.en]: "No search results.",
};

export default NO_RESULT_SEARCH;
